name: Deploy to cPanel

on:
  push:
    branches: [ master ]

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Deploy via SSH
        uses: appleboy/ssh-action@master
        with:
          host: 65.254.93.21
          username: act
          password: blaze4321!
          script: |
            cd /home/act/repositories/kpireqruit
            git pull origin master
            php -v
            echo "✅ Deployed latest commit to cPanel"
